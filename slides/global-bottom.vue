<template>
  <!-- Intentionally empty. The WebGL background lives inside each slide via
       <WebGLBackground /> in slides.md, so it is per-slide and scoped to the
       slide canvas rather than a global teleported element. -->
</template>
