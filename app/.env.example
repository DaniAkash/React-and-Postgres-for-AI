DATABASE_URL=postgres://repo:repo@localhost:5432/repo

# Set to true to make src/lib/embeddings.ts call a real model.
# Defaults to deterministic mock embeddings so the demo runs offline.
LIVE_AI=false
